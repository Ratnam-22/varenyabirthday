'use client';

import React from 'react';

export const BirthdayScene: React.FC = () => {
  return (
    <div data-testid="scene-birthday" className="scene-container">
      {/* Birthday Scene Placeholder */}
    </div>
  );
};

export default BirthdayScene;
