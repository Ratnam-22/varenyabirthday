import { EffectSettings } from '@/three/postprocessing/types';

export const cinematicEffectProfile: EffectSettings = {
  bloomEnabled: true,
  bloomIntensity: 1.2,
  dofEnabled: true,
  vignetteEnabled: true,
  noiseEnabled: true,
  chromaticAberrationEnabled: false,
};
