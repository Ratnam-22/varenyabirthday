import { EffectSettings } from '@/three/postprocessing/types';

export const minimalEffectProfile: EffectSettings = {
  bloomEnabled: false,
  bloomIntensity: 0,
  dofEnabled: false,
  vignetteEnabled: false,
  noiseEnabled: false,
  chromaticAberrationEnabled: false,
};
