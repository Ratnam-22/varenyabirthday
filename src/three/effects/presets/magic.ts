import { EffectSettings } from '@/three/postprocessing/types';

export const magicEffectProfile: EffectSettings = {
  bloomEnabled: true,
  bloomIntensity: 1.8,
  dofEnabled: false,
  vignetteEnabled: true,
  noiseEnabled: false,
  chromaticAberrationEnabled: true,
};
