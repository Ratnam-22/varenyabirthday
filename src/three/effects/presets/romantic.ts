import { EffectSettings } from '@/three/postprocessing/types';

export const romanticEffectProfile: EffectSettings = {
  bloomEnabled: true,
  bloomIntensity: 1.0,
  dofEnabled: false,
  vignetteEnabled: true,
  noiseEnabled: false,
  chromaticAberrationEnabled: false,
};
